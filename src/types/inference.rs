// Type inference engine — extends the type checker with Hindley-Milner style inference.
// For v1, the checker handles basic inference. This module is a placeholder for
// more advanced constraint-based inference in future versions.

pub use crate::types::checker::Type;
